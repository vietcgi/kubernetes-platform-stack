apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-certificate
  namespace: argocd
  labels:
    app.kubernetes.io/name: vault-certificate
    app.kubernetes.io/group: security
    app.kubernetes.io/managed-by: applications
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/vietcgi/kubernetes-platform-stack
    targetRevision: main
    path: manifests/vault
  destination:
    server: https://kubernetes.default.svc
    namespace: vault
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
