apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - _coredns.yaml
  - _argocd-config.yaml
  - _network-policies.yaml
  - _platform-apps.yaml
  - _infra-apps.yaml

sortOptions:
  order: fifo
